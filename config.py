# -*- coding: utf-8 -*-
"""全局配置：股票池、轮询间隔、策略开关"""
import json
import os

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
PROJECT_DIR = os.path.dirname(BASE_DIR)

# ---------------------------------------------------------------------------
# 数据源轮询节奏（单位：秒）
# ---------------------------------------------------------------------------
# 盘中实时快照轮询间隔（腾讯批量接口，一次可带多只股票）
SNAPSHOT_INTERVAL = 3.0
# 盘中分钟K线增量同步间隔
KLINE_SYNC_INTERVAL = 30.0
# 盘外（休市/周末）轮询间隔，降低频率避免打扰数据源
OFFLINE_SNAPSHOT_INTERVAL = 60.0
OFFLINE_KLINE_SYNC_INTERVAL = 300.0

# K线增量同步时每次拉取的根数
KLINE_FETCH_COUNT = 8
# 历史K线初始拉取根数
DAILY_HISTORY_LIMIT = 300

# ---------------------------------------------------------------------------
# 实时系统同步的K线周期
# ---------------------------------------------------------------------------
# history：首次启动拉取的根数
# every：盘中每 N 个同步周期拉一次（错峰，避免同一轮把所有周期都打出去）
# offline：休市时是否继续同步
#
# 不同步 1m：全仓库没有任何地方从数据库读 1m（K线图的 1m 走实时拉取），
# 它是每天约 28,800 行的纯写放大。历史上 1m 与 60m 共用一张无周期列的表，
# ts 撞主键导致当日 60m K线被静默顶掉——见 app/store.py 的 _migrate
REALTIME_PERIODS = {
    "daily": {"history": DAILY_HISTORY_LIMIT, "every": 2, "offline": True},
    "60m":   {"history": 240, "every": 2, "offline": False},
    "30m":   {"history": 480, "every": 2, "offline": False},   # 480根≈60个交易日
}

# 批量行情每批股票数（腾讯接口建议不超过 60）
SNAPSHOT_BATCH_SIZE = 50
# K线请求并发上限
KLINE_CONCURRENCY = 10
# HTTP 超时（秒）
HTTP_TIMEOUT = 8.0

# ---------------------------------------------------------------------------
# 股票池
# ---------------------------------------------------------------------------
# 内置精选股票池（仅沪深主板，不含创业板/科创板/北交所，剔除股价>100元的高价股）
# 被剔除的行业龙头已用同行业顺位强势股递补
BUILTIN_POOL = [
    ("002304", "洋河股份"), ("000858", "五粮液"), ("000568", "泸州老窖"), ("603369", "今世缘"),
    ("600036", "招商银行"), ("601318", "中国平安"), ("600030", "中信证券"), ("601166", "兴业银行"),
    ("600000", "浦发银行"), ("000001", "平安银行"), ("601398", "工商银行"), ("601288", "农业银行"),
    ("002709", "天赐材料"), ("002594", "比亚迪"), ("601012", "隆基绿能"), ("002475", "立讯精密"),
    ("601995", "中金公司"), ("600276", "恒瑞医药"), ("000739", "普洛药业"), ("600587", "新华医疗"),
    ("600887", "伊利股份"), ("603288", "海天味业"), ("600690", "海尔智家"), ("000333", "美的集团"),
    ("000651", "格力电器"), ("600900", "长江电力"), ("601088", "中国神华"), ("600028", "中国石化"),
    ("601857", "中国石油"), ("600585", "海螺水泥"), ("601668", "中国建筑"), ("600048", "保利发展"),
    ("000002", "万科A"), ("600383", "金地集团"), ("601899", "紫金矿业"), ("600111", "北方稀土"),
    ("601877", "正泰电器"), ("002230", "科大讯飞"), ("002185", "华天科技"), ("000021", "深科技"),
    ("002415", "海康威视"), ("603690", "至纯科技"), ("002156", "通富微电"), ("600498", "烽火通信"),
    ("600588", "用友网络"), ("002714", "牧原股份"), ("002311", "海大集团"), ("600438", "通威股份"),
    ("002460", "赣锋锂业"), ("603799", "华友钴业"), ("601985", "中国核电"), ("600089", "特变电工"),
    ("600104", "上汽集团"), ("601633", "长城汽车"), ("600741", "华域汽车"), ("002352", "顺丰控股"),
    ("601888", "中国中免"), ("600754", "锦江酒店"), ("601111", "中国国航"), ("600009", "上海机场"),
    ("601601", "中国太保"), ("601628", "中国人寿"), ("600901", "江苏金租"), ("601688", "华泰证券"),
    ("600837", "海通证券"), ("601211", "国泰君安"), ("000776", "广发证券"), ("601377", "兴业证券"),
    ("600763", "通策医疗"), ("000513", "丽珠集团"), ("603160", "汇顶科技"), ("002027", "分众传媒"),
    ("002007", "华兰生物"), ("600196", "复星医药"), ("600570", "恒生电子"), ("002142", "宁波银行"),
    ("601009", "南京银行"), ("600016", "民生银行"), ("601988", "中国银行"), ("601328", "交通银行"),
    ("600050", "中国联通"), ("600941", "中国移动"), ("601728", "中国电信"), ("600703", "三安光电"),
    ("002129", "TCL中环"), ("600845", "宝信软件"), ("002050", "三花智控"), ("600031", "三一重工"),
    ("601865", "福莱特"), ("000938", "紫光股份"), ("000066", "中国长城"), ("002920", "德赛西威"),
    ("603501", "韦尔股份"), ("600584", "长电科技"), ("002049", "紫光国微"), ("600460", "士兰微"),
    ("603019", "中科曙光"), ("000977", "浪潮信息"), ("002241", "歌尔股份"), ("002600", "领益智造"),
    # ↓↓ 板块拓展：影视/游戏/军工/船舶/航运/钢铁/化工/机器人/黄金/中药/通信 ↓↓
    ("002739", "儒意电影"), ("600977", "中国电影"), ("601595", "上海电影"), ("603103", "横店影视"),
    ("002555", "三七互娱"), ("002624", "完美世界"),
    ("600760", "中航沈飞"), ("600893", "航发动力"), ("600150", "中国船舶"), ("600862", "中航高科"),
    ("601919", "中远海控"), ("600018", "上港集团"),
    ("600019", "宝钢股份"), ("600309", "万华化学"), ("600426", "华鲁恒升"), ("002648", "卫星化学"),
    ("002747", "埃斯顿"), ("600547", "山东黄金"), ("600085", "同仁堂"),
    ("600406", "国电南瑞"), ("000063", "中兴通讯"),
]

# 是否自动从东方财富拉取沪深300成分股扩充股票池（失败时回退内置池）
ENABLE_HS300_POOL = False

# 配置文件（可选）：{"pool": [["600519","贵州茅台"], ...], "enable_hs300": true}
CONFIG_FILE = os.path.join(BASE_DIR, "user_pool.json")

# ---------------------------------------------------------------------------
# 数据库
# ---------------------------------------------------------------------------
DB_PATH = os.path.join(BASE_DIR, "kline.db")

# ---------------------------------------------------------------------------
# 用户自定义战法脚本
# ---------------------------------------------------------------------------
# 放 *.py，每个文件里用 @register_buy/@register_sell 注册 BuyRule/SellRule 子类，
# 改完跑 reload_rules.bat 生效（无需重启服务）。以 _ 开头的文件会被跳过
USER_RULES_DIR = os.path.join(BASE_DIR, "user_rules")

# 性能门禁：单只股票 5000 根K线内跑完 prepare()+全序列 on_bar() 的时间上限（毫秒）。
# 超限的规则不予注册——它几乎总是意味着指标在 on_bar 里重复计算（O(n²)）。
# 实测参照：6 个内置规则 3.5~20ms；把指标误写进 on_bar 的版本 8500ms
USER_RULE_BUDGET_MS = 500.0
# 门禁样本根数（从本地缓存里取最长的一条序列，不足则用实际根数归一化）
USER_RULE_SAMPLE_BARS = 5000

# 同一 (股票, 策略) 信号的最小重报间隔（秒）
SIGNAL_DEDUP_SECONDS = 30 * 60

# ---------------------------------------------------------------------------
# 把回测买入规则接到实时信号侦测
# ---------------------------------------------------------------------------
# 每条 = 一个实时信号源，出现在首页策略条上、可开关。
# 实时信号**全部**来自这里——原先写死在 app/strategies.py 的 8 个内置策略已删除：
# 3 个K线类与买入规则完全重复，5 个快照类中 4 个已改写成纯K线规则
# （volume_breakout/rsi_oversold/strong_up/limit_up），从此可回测；
# low_pe 因为 PE 无法从K线推导而放弃。
#   key     实时信号 key，必须唯一。实时去重是按 (股票, key) 做的，
#           所以同一规则注册多条（不同周期/参数）时每条都要独立 key，否则互相压制
#   rule    买入规则 key，取自 app/backtest/rules.py（含 user_rules/ 里自定义的）
#   period  用哪个周期的序列，必须是 REALTIME_PERIODS 里的周期
#   params  覆盖规则默认参数（扁平，与验证台参数面板一致）
#   name/desc  可选，缺省用规则自身的名字与 desc
#   confirm_on_close  True=判定最后一根【已收盘】K线（与回测逐根一致、报了不撤，
#                     显示为「买入战法」）；False=判定末根【进行中】K线
#                     （盘中即时、会撤，显示为「盘中异动」）
#   interval  仅盘中模式生效：每只股票最多每 N 秒求值一次，默认 30。
#             盘中模式每次求值都要重算 prepare()（末根的值一直在变，
#             不能靠 ts 做缓存），实测全池一轮 12~65ms，每 3 秒跑一遍是
#             0.4%~2.2% CPU 且阻塞事件循环。对延迟最敏感的信号才配小值
#
# 只支持买入规则：卖出规则的契约是"持仓中调用"，实时侧没有持仓概念
#
# 注意：本表在服务启动时读取。改这里要重启服务；
# reload_rules.bat 只重新加载 user_rules/ 里的规则**代码**并重新绑定，不重读本文件
REALTIME_RULE_SIGNALS = [
    # ---- 买入战法：收盘确认，与验证台回测逐根一致 ----
    {
        "key": "rt_kdj_rsi_30m",
        "rule": "kdj_rsi_golden",
        "period": "30m",
        "confirm_on_close": True,
        "params": {},
        "enabled": True,
    },
    # ---- 盘中异动：判定末根（进行中），语义等同原来的快照策略 ----
    # 日线末根在盘中会被实时快照刷新（见 Scanner._live_daily_bar），
    # 所以这些规则拿到的是 3 秒新鲜的当日数据
    {
        "key": "limit_up", "rule": "limit_up", "period": "daily",
        "confirm_on_close": False, "interval": 3,   # 涨停对延迟最敏感
        "params": {}, "enabled": True,
    },
    {
        "key": "strong_up", "rule": "strong_up", "period": "daily",
        "confirm_on_close": False,                  # interval 默认 30
        "params": {}, "enabled": True,
    },
    {
        "key": "volume_breakout", "rule": "volume_breakout", "period": "daily",
        "confirm_on_close": False,
        "params": {}, "enabled": True,
    },
    {
        "key": "rsi_oversold", "rule": "rsi_oversold", "period": "daily",
        "confirm_on_close": False,
        "params": {}, "enabled": True,
    },
]

# ---------------------------------------------------------------------------
# 回测默认配置（时间范围/周期等由代码控制，CLI 参数可覆盖）
# ---------------------------------------------------------------------------
BACKTEST_DEFAULT = {
    # 标的范围：pool=股票池 / watch=自选 / codes=指定代码
    "scope": "pool",
    "codes": [],                    # scope=codes 时生效，如 ["600519", "000858"]
    # 回测时间范围（YYYY-MM-DD；end 为空=数据最后一天）
    "start": "2025-01-01",
    "end": "",
    # K线周期：1m/5m/15m/30m/60m/daily/weekly
    "period": "30m",
    # 指标预热根数（自动向前多拉，不参与回测区间）
    "lookback": 300,
    # 初始资金与交易成本
    "init_cash": 1_000_000.0,
    "commission_rate": 0.001,       # 手续费 千分之一（买卖双向）
    "slippage_rate": 0.002,         # 滑点 千分之二（买价上浮/卖价下浮）
    # 策略与参数
    "strategy": "all_in_all_out",
    "params": {},
    # 引擎级通用风控（0=关闭；策略可通过信号自行卖出）
    "take_profit_pct": 0.0,         # 止盈，如 10.0 表示 +10%
    "stop_loss_pct": 0.0,           # 止损，如 5.0 表示 -5%
    "max_hold_bars": 0,             # 持有期上限（K线根数），0=不限制
    "max_positions": 5,             # 最大同时持仓数（0=不限制），多持仓战法适用
    # 成交价模式：close=信号当根收盘价 / next_open=下一根开盘价
    "exec_price": "close",
    # 单只股票最小数据根数（不足则跳过并警告）
    "min_bars": 60,
}


# ---------------------------------------------------------------------------
# 后台历史数据下载（python download.py / download.bat）
# ---------------------------------------------------------------------------
# 数据源固定为 BaoStock：唯一同时支持 start_date+end_date 区间查询、
# 不受单次根数上限约束、且日线/分钟线都返回真实成交额的源（前复权）
DOWNLOAD_DEFAULT = {
    # 标的范围：pool=股票池 / watch=自选 / all=全市场 / codes=指定代码
    "scope": "pool",
    "codes": [],                    # scope=codes 时生效，如 ["600519", "000858"]
    # 一次下载的K线周期列表（BaoStock 不支持 1m）
    "periods": ["30m", "daily"],
    # 下载区间（YYYY-MM-DD；end 为空=今天）
    "start": "2020-01-01",
    "end": "",
    # 更新方式：incremental=只补缺口（默认，每日更新用）/ full=整段重拉覆盖
    "mode": "incremental",
    # 每个任务之间的间隔（秒）；BaoStock 持锁期间内部还会再 sleep 0.5s
    "throttle": 0.5,
    # 失败清单输出文件（相对本文件所在目录）
    "failed_file": "download_failed.txt",
}


def load_user_pool() -> list:
    """读取用户配置文件中的股票池（若存在）"""
    if not os.path.exists(CONFIG_FILE):
        return []
    try:
        with open(CONFIG_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        pool = [(str(x[0]), str(x[1])) for x in data.get("pool", []) if len(x) >= 2]
        return pool
    except Exception:
        return []
